	if(CENTER_CONTROLE[7][CONTROL_STATE] == 1)
	{
		if(0 == SAMP_AC_CHECK_MODEL_PLANE(RAKNET_DRIVER[RAKNET_VEHICLE]) && !SAMP_AC_CHECK_MODEL_BOAT(RAKNET_DRIVER[RAKNET_VEHICLE]) && SAMP_AC_CHECK_MODEL_MOTO(RAKNET_DRIVER[RAKNET_VEHICLE]) == 0)
		{
			if(SERVER_INFO[playerid][SAMP_FUNC][1] != 0 && SERVER_INFO[playerid][SAMP_FUNC][2] != 0)
			{
				if(RAKNET_DRIVER[RAKNET_VEHICLE] != SERVER_INFO[playerid][SAMP_USE_VEHICLE] || SERVER_INFO[playerid][SAMP_TICK] < SERVER_INFO[playerid][SAMP_GTC_VEHICLE] + 500)
				{
					SAMP_AC_SEND_WARNING_FOR_SCRIPT(playerid, "Fast Enter Car", 7, 0);
				}
			}
		}
	}