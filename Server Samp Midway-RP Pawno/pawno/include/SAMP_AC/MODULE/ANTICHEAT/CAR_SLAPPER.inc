	if(CENTER_CONTROLE[22][CONTROL_STATE] == 1 && SAMP_AC_CHECK_MODEL_VEHICLE(RAKNET_DRIVER[RAKNET_VEHICLE]))
	{
		if(SERVER_INFO[playerid][SAMP_TICK] > SERVER_INFO[playerid][SAMP_FIX_SPEED] && RAKNET_DRIVER[RAKNET_SPEED_Z] > 3.50)
		{
			SAMP_AC_SEND_WARNING_FOR_SCRIPT(playerid, "Car Slapper", 22, 0); 
			return 0;
		}
	}
